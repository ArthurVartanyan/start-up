spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/mestogo
    username: postgres
    password: a7bgkKsr
  jpa:
    hibernate:
      ddl-auto: validate